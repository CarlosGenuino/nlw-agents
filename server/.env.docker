# Environment variables for Docker Compose
PORT=3000
DATABASE_URL=postgresql://docker:docker@agents-postgres:5432/agents
NODE_ENV=production

# Database configuration (used by postgres service)
POSTGRES_USER=docker
POSTGRES_PASSWORD=docker
POSTGRES_DB=agents